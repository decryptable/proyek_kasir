name: kasir_app
description: Production-ready terminal cashier simulation app in Dart.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dart_console: ^1.2.0
  interact: ^2.2.0
  intl: ^0.18.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
